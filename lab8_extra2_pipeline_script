stage('Run') {
    parallel(
        a: {
              node('master') { 
                  sh: 'echo $BUILD_NUMBER'; 
              }
        },
        b: {
              node('slave1_jenkins-06-02') { 
                  sh: 'date'; 
              }
        }
    )
}

