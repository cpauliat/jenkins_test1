node {
    stage('2 parallel tasks') {
      steps {
        parallel(
          a: {
              sh: 'echo JOB A $BUILD_NUMBER on $NODE_NAME'
          }
          b: {
              sh: 'date ; echo JOB B $NODE_NAME'
          }
        )
    }
}
