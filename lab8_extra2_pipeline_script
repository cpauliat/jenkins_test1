node {
    stage('2 parallel tasks') {
      steps {
        parallel(
          a: {
              sh: 'echo $BUILD_NUMBER'
          }
          b: {
              sh: 'date'
          }
        )
    }
}
